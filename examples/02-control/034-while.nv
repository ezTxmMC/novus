package main

// while repeats as long as its condition holds.

method main {
    var i = 1
    while (i <= 5) {
        println "step ${i}"
        i = i + 1
    }
    println "done after ${i - 1} steps"
}
