package main

// A method that calls itself needs a case that stops.

method factorial(integer n): integer {
    if (n <= 1) {
        return 1
    }
    return n * factorial(n - 1)
}

method main {
    println factorial(5)
    println factorial(10)
    println factorial(0)
}
