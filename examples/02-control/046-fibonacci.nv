package main

// Building the Fibonacci sequence with two running values.

method main {
    var a = 0
    var b = 1
    var out = []
    while (out.length() < 12) {
        out.append(a)
        var next = a + b
        a = b
        b = next
    }
    println out.join(" ")
}
